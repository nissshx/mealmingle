import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nissshh.mealmingle',
  appName: 'mealmingle',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
